name: flutter_listing_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  cached_network_image: ^3.4.0
  cloud_firestore: ^5.4.0
  dio: ^5.7.0
  firebase_core: ^3.4.0
  flutter:
    sdk: flutter
  flutter_inappwebview: ^6.1.3
  gap: ^3.0.1
  image_picker: ^1.1.2
  provider: ^6.1.2
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/logo/
    - assets/image/
